language: python
python:
  - "3.5"
  - "3.6"
  - "3.7"

before_install:
  - sudo apt-get update
  - sudo apt-get install build-essential libblas-dev liblapack-dev

script:
  - python setup.py develop
  - pytest -v
